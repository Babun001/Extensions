// chrome.runtime.onMessage.addListener((message, sender, sendResponse) => {
//     if (message === "getTabCount") {
//       chrome.tabs.query({}, (tabs) => {
//         sendResponse({ tabCount: tabs.length });
//       });
//       return true;
//     }
//   });
